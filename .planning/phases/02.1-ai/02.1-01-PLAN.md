---
phase: "02.1-ai"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/init.sql
  - backend/src/main/java/com/library/common/TfidfUtil.java
  - backend/src/main/java/com/library/service/impl/AISearchService.java
  - backend/src/main/java/com/library/controller/AISearchController.java
  - frontend/src/api/search.js
  - frontend/src/views/ai/SmartSearch.vue
  - frontend/src/router/index.js
  - frontend/src/views/Layout.vue
autonomous: true
requirements:
  - AI-01
must_haves:
  truths:
    - "User can search for books using natural language queries"
    - "Search results are ranked by relevance, not just keyword match"
    - "AI menu group appears in sidebar with Smart Search entry"
    - "Smart Search route is accessible at /ai/search"
    - "Admin role has permission to all AI menus"
  artifacts:
    - path: "backend/src/main/java/com/library/common/TfidfUtil.java"
      provides: "TF-IDF tokenization and cosine similarity computation"
      exports: ["tokenize", "computeTfidfVectors", "cosineSimilarity", "search"]
    - path: "backend/src/main/java/com/library/controller/AISearchController.java"
      provides: "POST /api/ai/search endpoint"
      exports: ["search"]
    - path: "frontend/src/views/ai/SmartSearch.vue"
      provides: "Smart search page with query input and relevance-ranked results"
      min_lines: 80
    - path: "frontend/src/router/index.js"
      provides: "Routes for /ai/search, /ai/recommend, /ai/hotbooks"
      contains: "ai/SmartSearch"
  key_links:
    - from: "frontend/src/views/ai/SmartSearch.vue"
      to: "POST /api/ai/search"
      via: "post from @/api/search"
      pattern: "import.*from.*@/api/search"
    - from: "frontend/src/views/Layout.vue"
      to: "/ai/search route"
      via: "el-menu-item index"
      pattern: "/ai/search"
    - from: "sql/init.sql"
      to: "sys_role_menu"
      via: "INSERT grant for role_id=1"
      pattern: "AI智能"
---

<objective>
建立 AI 功能基础设施（共享 TF-IDF 工具类、所有 AI 路由和菜单项），并实现 AI 智能搜索功能。

Purpose: 为后续 AI 功能（推荐、洞察）提供共享工具和基础设施，同时交付第一个可演示的 AI 特性。
Output: TF-IDF 工具类、AI 搜索后端 API、智能搜索前端页面、数据库菜单种子数据、路由和侧边栏菜单更新
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@sql/init.sql
@backend/src/main/java/com/library/common/Result.java
@backend/src/main/java/com/library/entity/Book.java
@backend/src/main/java/com/library/mapper/BookMapper.java
@backend/src/main/java/com/library/controller/BookController.java
@frontend/src/router/index.js
@frontend/src/views/Layout.vue
@frontend/src/api/request.js
@frontend/src/api/book.js
@frontend/src/views/book/Index.vue

<interfaces>
<!-- Key types and patterns the executor needs -->

From backend/src/main/java/com/library/common/Result.java:
```java
public class Result<T> {
    private Integer code; private String message; private T data;
    public static <T> Result<T> success(T data);
    public static <T> Result<T> success(String message, T data);
    public static <T> Result<T> error(String message);
    public static <T> Result<T> error(Integer code, String message);
}
```

From backend/src/main/java/com/library/entity/Book.java:
```java
@Data @TableName("sys_book")
public class Book {
    @TableId(type = IdType.AUTO) private Long id;
    private String title; private String author; private String isbn;
    private Long categoryId; private String description; private String coverUrl;
    private Integer stock; private Integer total; private Integer status;
    @TableLogic private Integer deleted;
    private LocalDateTime createTime; private LocalDateTime updateTime;
    @TableField(exist = false) private String categoryName;
}
```

From backend/src/main/java/com/library/mapper/BookMapper.java:
```java
@Mapper
public interface BookMapper extends BaseMapper<Book> {}
```

From frontend/src/api/request.js:
```javascript
// axios instance, baseURL: '/api', timeout: 10000, withCredentials: true
// Request interceptor adds Bearer token from localStorage
// Response interceptor unpacks response.data, shows ElMessage.error on failure
export default service;
```

From frontend/src/router/index.js:
```javascript
// Uses createRouter with createWebHistory
// Lazy-loaded components via () => import('...')
// beforeEach guard: redirect to /login if no token
const routes = [ /* existing routes */ ];
```

From frontend/src/views/Layout.vue:
```vue
<!-- Sidebar uses el-menu with router attribute -->
<!-- el-sub-menu for groups, el-menu-item for pages -->
<!-- Icons imported from @element-plus/icons-vue -->
<!-- Existing groups: /system (系统管理), /library (图书馆业务) -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 数据库种子数据 — 添加 AI 功能菜单</name>
  <files>sql/init.sql</files>
  <read_first>
    <file path="/Users/ghost/Code/Homework/web-end/sql/init.sql">Read the full current SQL file to understand existing menu structure and append point</file>
  </read_first>
  <action>
在 sql/init.sql 文件末尾（最后一行已有数据之后）追加以下 SQL 语句块：

```sql
-- AI智能菜单
INSERT INTO sys_menu (menu_name, parent_id, path, component, icon, sort, perms, menu_type) VALUES
('AI智能', 0, '/ai', NULL, 'Cpu', 3, NULL, 1);

SET @ai_parent_id = LAST_INSERT_ID();

INSERT INTO sys_menu (menu_name, parent_id, path, component, icon, sort, perms, menu_type) VALUES
('智能搜索', @ai_parent_id, '/ai/search', 'ai/SmartSearch', 'Search', 1, 'ai:search', 1),
('图书推荐', @ai_parent_id, '/ai/recommend', 'ai/Recommend', 'Star', 2, 'ai:recommend', 1),
('借阅洞察', @ai_parent_id, '/ai/hotbooks', 'ai/HotBooks', 'TrendCharts', 3, 'ai:insight', 1);

-- 管理员角色（role_id=1）拥有所有AI菜单权限
INSERT INTO sys_role_menu (role_id, menu_id)
SELECT 1, id FROM sys_menu WHERE menu_name IN ('AI智能', '智能搜索', '图书推荐', '借阅洞察');
```

注意：
- parent_id=0 表示顶级菜单
- sort=3 将 AI 智能放在首页(0)、系统管理(1)、图书馆业务(2)之后
- 子菜单的 parent_id 使用 @ai_parent_id 变量引用刚插入的父菜单 ID
- menu_type=1 表示菜单类型；perms 用于按钮级权限标识
</action>
  <acceptance_criteria>
    <criterion type="grep">sql/init.sql 包含 "INSERT INTO sys_menu" 且包含 "'AI智能'"</criterion>
    <criterion type="grep">sql/init.sql 包含 "'智能搜索'" 且包含 "'ai:search'"</criterion>
    <criterion type="grep">sql/init.sql 包含 "'图书推荐'" 且包含 "'ai:recommend'"</criterion>
    <criterion type="grep">sql/init.sql 包含 "'借阅洞察'" 且包含 "'ai:insight'"</criterion>
    <criterion type="grep">sql/init.sql 包含 "@ai_parent_id"</criterion>
    <criterion type="grep">sql/init.sql 包含 "INSERT INTO sys_role_menu" 且包含 "AI智能"</criterion>
  </acceptance_criteria>
  <verify>
    <automated>grep -c "'AI智能'" /Users/ghost/Code/Homework/web-end/sql/init.sql | xargs -I{} sh -c 'test {} -ge 2'</automated>
  </verify>
  <done>init.sql 末尾追加了 4 条菜单 INSERT +角色授权 INSERT，共 5 条新 SQL 语句</done>
</task>

<task type="auto">
  <name>Task 2: 后端 — TF-IDF 工具类 + AI 智能搜索服务</name>
  <files>
    backend/src/main/java/com/library/common/TfidfUtil.java,
    backend/src/main/java/com/library/service/impl/AISearchService.java,
    backend/src/main/java/com/library/controller/AISearchController.java
  </files>
  <read_first>
    <file path="/Users/ghost/Code/Homework/web-end/backend/src/main/java/com/library/common/Result.java">了解 Result 泛型封装类的结构</file>
    <file path="/Users/ghost/Code/Homework/web-end/backend/src/main/java/com/library/entity/Book.java">了解 Book 实体的字段名（title, author, description, isbn, categoryId 等）</file>
    <file path="/Users/ghost/Code/Homework/web-end/backend/src/main/java/com/library/mapper/BookMapper.java">了解 BookMapper 继承自 BaseMapper</file>
  </read_first>
  <action>

**步骤 1：创建 `backend/src/main/java/com/library/common/TfidfUtil.java`**

这是一个纯静态工具类，提供中文文本的 TF-IDF 向量化和余弦相似度计算。无外部依赖，仅使用 Java 标准库。

```java
package com.library.common;

import java.util.*;
import java.util.stream.Collectors;

/**
 * TF-IDF 文本相似度工具类
 * 对中文文本使用字符二元组（bigram）分词，对英文使用空格分词
 */
public class TfidfUtil {

    /**
     * 对文本进行分词
     * - 中文：连续汉字提取2字符滑动窗口（bigram）
     * - 英文：按空格拆分单词，保留长度>=2的纯字母单词并转小写
     * - 过滤标点符号和空白
     */
    public static List<String> tokenize(String text) {
        if (text == null || text.isEmpty()) return Collections.emptyList();
        List<String> tokens = new ArrayList<>();
        // 移除标点符号，保留字母、数字、中文、空格
        String cleaned = text.replaceAll("[\\p{P}\\p{S}]", " ").trim();
        if (cleaned.isEmpty()) return tokens;
        // 提取英文单词（长度>=2）
        String[] words = cleaned.split("\\s+");
        for (String word : words) {
            if (word.length() >= 2 && word.matches("[a-zA-Z]+")) {
                tokens.add(word.toLowerCase());
            }
        }
        // 提取中文连续序列并做 bigram 分词
        StringBuilder chineseSeq = new StringBuilder();
        for (char c : cleaned.toCharArray()) {
            if (c >= '\u4e00' && c <= '\u9fff') {
                chineseSeq.append(c);
            }
        }
        String chinese = chineseSeq.toString();
        for (int i = 0; i < chinese.length() - 1; i++) {
            tokens.add(chinese.substring(i, i + 2));
        }
        return tokens;
    }

    /**
     * 计算单个文档的 TF（词频）向量
     * TF = 词在文档中出现的次数 / 文档总词数
     */
    public static Map<String, Double> computeTf(List<String> tokens) {
        Map<String, Double> tf = new HashMap<>();
        if (tokens.isEmpty()) return tf;
        Map<String, Long> counts = tokens.stream()
                .collect(Collectors.groupingBy(t -> t, Collectors.counting()));
        double total = tokens.size();
        for (Map.Entry<String, Long> entry : counts.entrySet()) {
            tf.put(entry.getKey(), entry.getValue() / total);
        }
        return tf;
    }

    /**
     * 为所有文档计算 TF-IDF 向量
     * @param docs 文档映射：文档ID -> 分词结果
     * @return 文档ID -> (词 -> TF-IDF分数)
     */
    public static Map<String, Map<String, Double>> computeTfidfVectors(Map<String, List<String>> docs) {
        // 先计算每个文档的 TF
        List<Map.Entry<String, Map<String, Double>>> tfList = new ArrayList<>();
        for (Map.Entry<String, List<String>> entry : docs.entrySet()) {
            Map<String, Double> tf = computeTf(entry.getValue());
            tfList.add(new AbstractMap.SimpleEntry<>(entry.getKey(), tf));
        }
        // 计算 IDF：log(总文档数 / 包含该词的文档数)
        int N = tfList.size();
        Map<String, Integer> docFreq = new HashMap<>();
        for (Map.Entry<String, Map<String, Double>> docEntry : tfList) {
            for (String term : docEntry.getValue().keySet()) {
                docFreq.merge(term, 1, Integer::sum);
            }
        }
        Map<String, Double> idf = new HashMap<>();
        for (Map.Entry<String, Integer> entry : docFreq.entrySet()) {
            idf.put(entry.getKey(), Math.log((double) N / entry.getValue()));
        }
        // TF * IDF
        Map<String, Map<String, Double>> tfidf = new LinkedHashMap<>();
        for (Map.Entry<String, Map<String, Double>> docEntry : tfList) {
            Map<String, Double> vec = new HashMap<>();
            for (Map.Entry<String, Double> termEntry : docEntry.getValue().entrySet()) {
                String term = termEntry.getKey();
                double score = termEntry.getValue() * idf.getOrDefault(term, 0.0);
                vec.put(term, score);
            }
            tfidf.put(docEntry.getKey(), vec);
        }
        return tfidf;
    }

    /**
     * 计算两个 TF-IDF 向量之间的余弦相似度
     */
    public static double cosineSimilarity(Map<String, Double> v1, Map<String, Double> v2) {
        if (v1.isEmpty() || v2.isEmpty()) return 0.0;
        double dotProduct = 0.0;
        double norm1 = 0.0;
        double norm2 = 0.0;
        // v1 的范数
        for (double val : v1.values()) norm1 += val * val;
        norm1 = Math.sqrt(norm1);
        // v2 的范数
        for (double val : v2.values()) norm2 += val * val;
        norm2 = Math.sqrt(norm2);
        if (norm1 == 0.0 || norm2 == 0.0) return 0.0;
        // 点积：遍历较小的向量以提高效率
        Map<String, Double> smaller = v1.size() <= v2.size() ? v1 : v2;
        Map<String, Double> larger = v1.size() <= v2.size() ? v2 : v1;
        for (Map.Entry<String, Double> entry : smaller.entrySet()) {
            Double val2 = larger.get(entry.getKey());
            if (val2 != null) {
                dotProduct += entry.getValue() * val2;
            }
        }
        return dotProduct / (norm1 * norm2);
    }

    /**
     * 搜索：将 query 分词后计算 TF 向量，与预计算的文档 TF-IDF 向量做余弦相似度，
     * 返回按相似度降序排列的文档 ID 列表（只返回相似度 > 0 的结果）
     */
    public static List<Map.Entry<String, Double>> search(String query, Map<String, Map<String, Double>> docVectors) {
        List<String> queryTokens = tokenize(query);
        if (queryTokens.isEmpty()) return Collections.emptyList();
        Map<String, Double> queryVec = computeTf(queryTokens);
        List<Map.Entry<String, Double>> results = new ArrayList<>();
        for (Map.Entry<String, Map<String, Double>> docEntry : docVectors.entrySet()) {
            double sim = cosineSimilarity(queryVec, docEntry.getValue());
            if (sim > 0) {
                results.add(new AbstractMap.SimpleEntry<>(docEntry.getKey(), sim));
            }
        }
        results.sort((a, b) -> Double.compare(b.getValue(), a.getValue()));
        return results;
    }
}
```

**步骤 2：创建 `backend/src/main/java/com/library/service/impl/AISearchService.java`**

注意：不使用 @Service 注解加实现类后缀的模式，改为直接用 @Service 命名类；遵循项目已有的 ServiceImpl 命名惯例。

```java
package com.library.service.impl;

import com.library.common.TfidfUtil;
import com.library.entity.Book;
import com.library.mapper.BookMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import jakarta.annotation.PostConstruct;
import java.util.*;
import java.util.stream.Collectors;

@Service
public class AISearchService {

    @Autowired
    private BookMapper bookMapper;

    /** 缓存：图书 ID（字符串） -> TF-IDF 向量 */
    private Map<String, Map<String, Double>> bookVectors = new LinkedHashMap<>();
    /** 缓存：图书 ID（字符串） -> Book 对象 */
    private Map<String, Book> bookCache = new LinkedHashMap<>();
    /** 是否已初始化 */
    private volatile boolean initialized = false;

    /**
     * 启动时预计算所有图书的 TF-IDF 向量
     */
    @PostConstruct
    public void init() {
        rebuildIndex();
    }

    /**
     * 重建索引：从数据库加载所有未删除图书，构建 TF-IDF 向量索引
     */
    public synchronized void rebuildIndex() {
        List<Book> books = bookMapper.selectList(null);
        Map<String, List<String>> docs = new LinkedHashMap<>();
        bookCache.clear();
        for (Book book : books) {
            String docId = book.getId().toString();
            // 组合书名、作者、描述 作为文档内容
            String text = String.join(" ",
                    book.getTitle() != null ? book.getTitle() : "",
                    book.getAuthor() != null ? book.getAuthor() : "",
                    book.getDescription() != null ? book.getDescription() : ""
            );
            docs.put(docId, TfidfUtil.tokenize(text));
            bookCache.put(docId, book);
        }
        this.bookVectors = TfidfUtil.computeTfidfVectors(docs);
        this.initialized = true;
    }

    /**
     * 搜索：返回按相似度排序的 Book + score 列表
     * @param query 用户查询文本
     * @param limit 返回结果最大数量
     */
    public List<Map<String, Object>> search(String query, int limit) {
        if (query == null || query.trim().isEmpty()) {
            return Collections.emptyList();
        }
        if (!initialized) {
            rebuildIndex();
        }
        List<Map.Entry<String, Double>> ranked = TfidfUtil.search(query.trim(), bookVectors);
        List<Map<String, Object>> results = new ArrayList<>();
        for (Map.Entry<String, Double> entry : ranked) {
            if (results.size() >= limit) break;
            Book book = bookCache.get(entry.getKey());
            if (book != null) {
                Map<String, Object> item = new LinkedHashMap<>();
                item.put("id", book.getId());
                item.put("title", book.getTitle());
                item.put("author", book.getAuthor());
                item.put("isbn", book.getIsbn());
                item.put("categoryId", book.getCategoryId());
                item.put("description", book.getDescription());
                item.put("coverUrl", book.getCoverUrl());
                item.put("stock", book.getStock());
                item.put("status", book.getStatus());
                item.put("score", Math.round(entry.getValue() * 10000.0) / 10000.0);
                results.add(item);
            }
        }
        return results;
    }

    /**
     * 获取索引中的图书总数
     */
    public int getIndexedCount() {
        return bookCache.size();
    }
}
```

**步骤 3：创建 `backend/src/main/java/com/library/controller/AISearchController.java`**

```java
package com.library.controller;

import com.library.common.Result;
import com.library.service.impl.AISearchService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

import java.util.List;
import java.util.Map;

@RestController
@RequestMapping("/api/ai")
public class AISearchController {

    @Autowired
    private AISearchService aiSearchService;

    /**
     * AI 智能搜索
     * POST /api/ai/search
     * Body: { "query": "科幻小说", "limit": 10 }
     */
    @PostMapping("/search")
    public Result<List<Map<String, Object>>> search(@RequestBody Map<String, Object> params) {
        String query = (String) params.getOrDefault("query", "");
        int limit = params.containsKey("limit") ? ((Number) params.get("limit")).intValue() : 10;
        List<Map<String, Object>> results = aiSearchService.search(query, limit);
        return Result.success(results);
    }

    /**
     * 重建搜索索引
     * POST /api/ai/rebuild-index
     */
    @PostMapping("/rebuild-index")
    public Result<Map<String, Object>> rebuildIndex() {
        aiSearchService.rebuildIndex();
        Map<String, Object> info = new java.util.LinkedHashMap<>();
        info.put("indexedCount", aiSearchService.getIndexedCount());
        info.put("status", "ok");
        return Result.success("索引重建成功", info);
    }
}
```
</action>
  <acceptance_criteria>
    <criterion type="grep">backend/src/main/java/com/library/common/TfidfUtil.java 包含 "public static" 且包含 "tokenize"</criterion>
    <criterion type="grep">backend/src/main/java/com/library/common/TfidfUtil.java 包含 "computeTfidfVectors"</criterion>
    <criterion type="grep">backend/src/main/java/com/library/common/TfidfUtil.java 包含 "cosineSimilarity"</criterion>
    <criterion type="grep">backend/src/main/java/com/library/common/TfidfUtil.java 包含 "public static List.*search"</criterion>
    <criterion type="grep">backend/src/main/java/com/library/service/impl/AISearchService.java 包含 "@PostConstruct"</criterion>
    <criterion type="grep">backend/src/main/java/com/library/service/impl/AISearchService.java 包含 "rebuildIndex"</criterion>
    <criterion type="grep">backend/src/main/java/com/library/service/impl/AISearchService.java 包含 "TfidfUtil.tokenize"</criterion>
    <criterion type="grep">backend/src/main/java/com/library/controller/AISearchController.java 包含 "@RequestMapping(\"/api/ai\")"</criterion>
    <criterion type="grep">backend/src/main/java/com/library/controller/AISearchController.java 包含 "@PostMapping(\"/search\")"</criterion>
    <criterion type="grep">backend/src/main/java/com/library/controller/AISearchController.java 包含 "aiSearchService.search"</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ghost/Code/Homework/web-end/backend && mvn compile -q 2>&1</automated>
  </verify>
  <done>三个 Java 文件创建完毕，mvn compile 通过（BUILD SUCCESS）</done>
</task>

<task type="auto">
  <name>Task 3: 前端 — AI 路由、菜单、智能搜索页面</name>
  <files>
    frontend/src/router/index.js,
    frontend/src/views/Layout.vue,
    frontend/src/api/search.js,
    frontend/src/views/ai/SmartSearch.vue
  </files>
  <read_first>
    <file path="/Users/ghost/Code/Homework/web-end/frontend/src/router/index.js">了解路由结构、lazy import 模式和 beforeEach 守卫</file>
    <file path="/Users/ghost/Code/Homework/web-end/frontend/src/views/Layout.vue">了解sidebar菜单结构、el-sub-menu和el-menu-item的写法、icon导入方式</file>
    <file path="/Users/ghost/Code/Homework/web-end/frontend/src/api/book.js">了解API模块的函数签名模式（从request导入，返回request调用）</file>
    <file path="/Users/ghost/Code/Homework/web-end/frontend/src/views/book/Index.vue">了解现有页面的 Element Plus 组件使用模式（el-table, el-pagination, el-input, el-tag等）</file>
  </read_first>
  <action>

**步骤 1：修改 `frontend/src/router/index.js`**

在 children 数组中、"// 图书馆业务" 注释之前，添加 AI 路由块。找到 `{ path: '/category', ... }` 这一行，在它**之前**插入以下三个路由：

```javascript
// AI智能
{ path: '/ai/search', name: 'SmartSearch', component: () => import('../views/ai/SmartSearch.vue') },
{ path: '/ai/recommend', name: 'Recommend', component: () => import('../views/ai/Recommend.vue') },
{ path: '/ai/hotbooks', name: 'HotBooks', component: () => import('../views/ai/HotBooks.vue') },
```

完整上下文（插入后的 children 数组）：
```javascript
children: [
  { path: '/dashboard', name: 'Dashboard', component: () => import('../views/dashboard/Index.vue') },
  // 系统管理
  { path: '/system/user', name: 'UserManage', component: () => import('../views/system/UserManage.vue') },
  { path: '/system/role', name: 'RoleManage', component: () => import('../views/system/RoleManage.vue') },
  { path: '/system/menu', name: 'MenuManage', component: () => import('../views/system/MenuManage.vue') },
  // AI智能
  { path: '/ai/search', name: 'SmartSearch', component: () => import('../views/ai/SmartSearch.vue') },
  { path: '/ai/recommend', name: 'Recommend', component: () => import('../views/ai/Recommend.vue') },
  { path: '/ai/hotbooks', name: 'HotBooks', component: () => import('../views/ai/HotBooks.vue') },
  // 图书馆业务
  { path: '/category', name: 'Category', component: () => import('../views/category/Index.vue') },
  // ... 其余路由不变
]
```

**步骤 2：修改 `frontend/src/views/Layout.vue`**

2a. 在 `<script setup>` 中，将 icon 导入行从：
```javascript
import { HomeFilled, Setting, Reading } from '@element-plus/icons-vue'
```
改为（添加 Cpu, Star, Search, TrendCharts）：
```javascript
import { HomeFilled, Setting, Reading, Cpu, Star, Search, TrendCharts } from '@element-plus/icons-vue'
```

2b. 在 sidebar 的 `<el-menu>` 中，在"图书馆业务" el-sub-menu **之前**（即 `</el-sub-menu>` 关闭标签与 `<el-sub-menu index="/library">` 之间），插入 AI 智能菜单组：

```html
<el-sub-menu index="/ai">
  <template #title>
    <el-icon><Cpu /></el-icon>
    <span>AI智能</span>
  </template>
  <el-menu-item index="/ai/search">
    <el-icon><Search /></el-icon>
    <span>智能搜索</span>
  </el-menu-item>
  <el-menu-item index="/ai/recommend">
    <el-icon><Star /></el-icon>
    <span>图书推荐</span>
  </el-menu-item>
  <el-menu-item index="/ai/hotbooks">
    <el-icon><TrendCharts /></el-icon>
    <span>借阅洞察</span>
  </el-menu-item>
</el-sub-menu>
```

**步骤 3：创建 `frontend/src/api/search.js`**

```javascript
import request from './request'

/**
 * AI 智能搜索
 * @param {Object} params - { query: string, limit: number }
 * @returns {Promise} - { code: 200, data: [{ id, title, author, isbn, categoryId, description, coverUrl, stock, status, score }] }
 */
export function aiSearch(params) {
  return request({ url: '/ai/search', method: 'post', data: params })
}

/**
 * 重建搜索索引（管理员用）
 */
export function rebuildIndex() {
  return request({ url: '/ai/rebuild-index', method: 'post' })
}
```

**步骤 4：创建 `frontend/src/views/ai/SmartSearch.vue`**

这是一个完整的 Vue 3 + Element Plus 页面组件，功能：
- 顶部搜索栏：输入框 + 搜索按钮 + 重建索引按钮（仅管理员可见）
- 搜索结果以表格展示：排名、书名、作者、ISBN、库存、状态、相关度分数
- 点击书名可跳转到图书详情页 /book/:id
- 空状态：未搜索时显示提示，搜索无结果时显示 Empty

```vue
<template>
  <div class="page-container">
    <div class="page-card">
      <div class="page-card-header">
        <span class="page-title">AI 智能搜索</span>
        <span style="font-size:13px;color:var(--color-text-muted);margin-left:12px">
          支持自然语言搜索，基于 TF-IDF 语义匹配
        </span>
      </div>
      <div class="page-card-body">
        <!-- 搜索栏 -->
        <div class="search-bar" style="margin-bottom:20px;display:flex;gap:12px;align-items:center">
          <el-input
            v-model="query"
            placeholder="输入关键词描述，例如：科幻小说、中国古典名著、历史类书籍..."
            style="flex:1;max-width:560px"
            clearable
            @keyup.enter="handleSearch"
          />
          <el-button type="primary" :loading="loading" @click="handleSearch">
            <el-icon style="margin-right:4px"><Search /></el-icon>
            搜索
          </el-button>
          <el-button v-if="isAdmin" @click="handleRebuild" :loading="rebuilding" size="small" style="margin-left:4px">
            重建索引
          </el-button>
        </div>

        <!-- 结果表格 -->
        <el-table v-if="searched" :data="results" stripe empty-text="未找到匹配的图书，请尝试其他关键词">
          <el-table-column type="index" label="#" width="50" />
          <el-table-column prop="title" label="书名" min-width="160">
            <template #default="{ row }">
              <el-link type="primary" @click="$router.push(`/book/${row.id}`)">{{ row.title }}</el-link>
            </template>
          </el-table-column>
          <el-table-column prop="author" label="作者" width="120" />
          <el-table-column prop="isbn" label="ISBN" width="150" />
          <el-table-column prop="stock" label="库存" width="60" />
          <el-table-column prop="status" label="状态" width="80">
            <template #default="{ row }">
              <el-tag :type="row.status === 1 ? 'success' : 'danger'" size="small">
                {{ row.status === 1 ? '上架' : '下架' }}
              </el-tag>
            </template>
          </el-table-column>
          <el-table-column prop="score" label="相关度" width="90">
            <template #default="{ row }">
              <span :style="{ color: row.score > 0.1 ? '#22c55e' : '#94a3b8' }">
                {{ (row.score * 100).toFixed(1) }}%
              </span>
            </template>
          </el-table-column>
        </el-table>

        <!-- 搜索前提示 -->
        <el-empty v-if="!searched" description="输入关键词开始 AI 智能搜索" :image-size="120" />
      </div>
    </div>
  </div>
</template>

<script setup>
import { ref } from 'vue'
import { Search } from '@element-plus/icons-vue'
import { ElMessage } from 'element-plus'
import { aiSearch, rebuildIndex } from '@/api/search'
import { useUserStore } from '@/stores/user'

const userStore = useUserStore()
const query = ref('')
const results = ref([])
const loading = ref(false)
const rebuilding = ref(false)
const searched = ref(false)

/** 判断是否为管理员（role_id=1） */
const isAdmin = ref(userStore.userInfo?.roleId === 1 || userStore.userInfo?.roleName === '管理员')

const handleSearch = async () => {
  if (!query.value.trim()) {
    ElMessage.warning('请输入搜索关键词')
    return
  }
  loading.value = true
  try {
    const res = await aiSearch({ query: query.value.trim(), limit: 20 })
    if (res.code === 200) {
      results.value = res.data || []
    }
    searched.value = true
  } catch (e) {
    ElMessage.error(e.message || '搜索失败')
  } finally {
    loading.value = false
  }
}

const handleRebuild = async () => {
  rebuilding.value = true
  try {
    const res = await rebuildIndex()
    if (res.code === 200) {
      ElMessage.success(`索引重建完成，共 ${res.data?.indexedCount || 0} 本图书`)
    }
  } catch (e) {
    ElMessage.error('索引重建失败')
  } finally {
    rebuilding.value = false
  }
}
</script>
```

注意：以上 Vue 模板中的 `el-table-column type="index"` 可能被 TS 检查标记但实际可用。如果 Element Plus 版本不支持 `type="index"`，改为使用 `#default="{ $index }"` 插槽手动渲染 `{{ $index + 1 }}`。
</action>
  <acceptance_criteria>
    <criterion type="grep">frontend/src/router/index.js 包含 "ai/SmartSearch"</criterion>
    <criterion type="grep">frontend/src/router/index.js 包含 "ai/Recommend"</criterion>
    <criterion type="grep">frontend/src/router/index.js 包含 "ai/HotBooks"</criterion>
    <criterion type="grep">frontend/src/views/Layout.vue 包含 "Cpu"（在 import 语句中）</criterion>
    <criterion type="grep">frontend/src/views/Layout.vue 包含 "index=\"/ai\""</criterion>
    <criterion type="grep">frontend/src/views/Layout.vue 包含 "/ai/search"</criterion>
    <criterion type="grep">frontend/src/views/Layout.vue 包含 "/ai/recommend"</criterion>
    <criterion type="grep">frontend/src/views/Layout.vue 包含 "/ai/hotbooks"</criterion>
    <criterion type="grep">frontend/src/api/search.js 包含 "export function aiSearch"</criterion>
    <criterion type="grep">frontend/src/views/ai/SmartSearch.vue 包含 "aiSearch"</criterion>
    <criterion type="grep">frontend/src/views/ai/SmartSearch.vue 包含 "handleSearch"</criterion>
    <criterion type="grep">frontend/src/views/ai/SmartSearch.vue 包含 "相关度"</criterion>
    <criterion type="grep">frontend/src/views/ai/SmartSearch.vue 包含 "@/api/search"</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ghost/Code/Homework/web-end/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>前端路由和菜单包含全部 3 个 AI 页面入口；SmartSearch.vue 页面可进行 TF-IDF 语义搜索并显示相关度排序结果；npm run build 成功</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> POST /api/ai/search | 用户输入的搜索查询文本跨越信任边界进入后端处理 |
| Client -> POST /api/ai/rebuild-index | 重建索引操作可触发全表扫描，需要限制访问 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-01 | Denial of Service | AISearchController.search() | mitigate | 对 search() 方法中的 limit 参数设置上限（max 50），防止恶意请求触发全量计算 |
| T-02.1-02 | Information Disclosure | AISearchController.rebuildIndex() | mitigate | rebuildIndex 端点仅对管理员开放；在控制器方法中检查用户角色（通过 X-User-Id header + UserMapper 查询 role_id=1），非管理员返回 403 |
| T-02.1-03 | Tampering | POST /api/ai/search body | accept | 搜索功能为只读操作，无数据修改风险；即使用户提交恶意 JSON payload，Jackson 反序列化会安全处理 |
</threat_model>

<verification>
整体验证步骤：
1. 后端编译：`cd backend && mvn compile -q` 应输出 BUILD SUCCESS
2. 前端构建：`cd frontend && npm run build` 应成功完成
3. 数据库种子数据检查：init.sql 包含 AI 菜单的 INSERT 语句和角色授权
4. 路由完整性：router/index.js 包含 3 个 /ai/* 路由
5. 菜单完整性：Layout.vue 包含 AI 智能菜单组及 3 个子菜单项
</verification>

<success_criteria>
1. `mvn compile -q` 返回 BUILD SUCCESS，无编译错误
2. `npm run build` 成功完成，无构建错误
3. 前端侧边栏显示"AI智能"菜单组，包含"智能搜索"、"图书推荐"、"借阅洞察"三个子菜单项
4. 访问 /ai/search 页面，输入"科幻"后搜索，返回按相关度排序的图书列表（三体排在前面）
5. POST /api/ai/search body={"query":"科幻","limit":10} 返回 code=200，data 数组中每项包含 id, title, author, score 字段
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-ai/02.1-01-SUMMARY.md`
</output>
