---
phase: "02.1-ai"
plan: "02"
type: execute
wave: 2
depends_on:
  - "02.1-01"
files_modified:
  - backend/src/main/java/com/library/service/impl/RecommendService.java
  - backend/src/main/java/com/library/controller/RecommendController.java
  - frontend/src/api/recommend.js
  - frontend/src/views/ai/Recommend.vue
  - frontend/src/views/book/Detail.vue
autonomous: true
requirements:
  - AI-02
must_haves:
  truths:
    - "User can see book recommendations on the dedicated Recommend page"
    - "Recommendations are based on content similarity (TF-IDF + cosine similarity)"
    - "Book Detail page shows a 'Similar Books' section with AI-recommended books"
    - "Recommendations include a similarity score"
  artifacts:
    - path: "backend/src/main/java/com/library/service/impl/RecommendService.java"
      provides: "Content-based and popularity-based book recommendation"
      exports: ["recommendByBook", "recommendByHistory", "getHotBooks"]
    - path: "backend/src/main/java/com/library/controller/RecommendController.java"
      provides: "GET /api/ai/recommend/book/{id}, GET /api/ai/recommend/user, GET /api/ai/hot"
    - path: "frontend/src/views/ai/Recommend.vue"
      provides: "图书推荐页面，展示历史借阅推荐和热门图书"
      min_lines: 100
    - path: "frontend/src/views/book/Detail.vue"
      provides: "相似图书推荐区块"
      contains: "recommendByBook"
  key_links:
    - from: "RecommendService.java"
      to: "TfidfUtil.java"
      via: "import com.library.common.TfidfUtil"
      pattern: "import.*TfidfUtil"
    - from: "RecommendController.java"
      to: "RecommendService.java"
      via: "@Autowired RecommendService"
      pattern: "RecommendService"
    - from: "frontend/src/views/book/Detail.vue"
      to: "GET /api/ai/recommend/book/{id}"
      via: "getSimilarBooks() from @/api/recommend"
      pattern: "import.*from.*@/api/recommend"
    - from: "frontend/src/views/ai/Recommend.vue"
      to: "RecommendController endpoints"
      via: "recommend.js API module"
      pattern: "import.*from.*@/api/recommend"
---
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/src/main/java/com/library/common/TfidfUtil.java
@backend/src/main/java/com/library/common/Result.java
@backend/src/main/java/com/library/entity/Book.java
@backend/src/main/java/com/library/entity/Borrow.java
@backend/src/main/java/com/library/mapper/BookMapper.java
@backend/src/main/java/com/library/mapper/BorrowMapper.java
@backend/src/main/java/com/library/controller/BorrowController.java
@frontend/src/api/book.js
@frontend/src/api/request.js
@frontend/src/views/book/Detail.vue
@frontend/src/views/book/Index.vue

<interfaces>
<!-- Key types the executor needs from Plan 01 -->

From TfidfUtil.java (created in Plan 01):
```java
public class TfidfUtil {
    public static List<String> tokenize(String text);
    public static Map<String, Double> computeTf(List<String> tokens);
    public static Map<String, Map<String, Double>> computeTfidfVectors(Map<String, List<String>> docs);
    public static double cosineSimilarity(Map<String, Double> v1, Map<String, Double> v2);
}
```

From Book.java:
```java
@Data @TableName("sys_book")
public class Book {
    private Long id; private String title; private String author;
    private String isbn; private Long categoryId; private String description;
    private String coverUrl; private Integer stock; private Integer total;
    private Integer status; @TableLogic private Integer deleted;
    @TableField(exist = false) private String categoryName;
}
```

From BorrowMapper.java (custom SQL methods):
```java
@Mapper
public interface BorrowMapper extends BaseMapper<Borrow> {
    @Select("...") List<Borrow> selectByUserId(@Param("userId") Long userId);
    @Select("...") List<Borrow> selectAll();
}
```

From BorrowController.java (how user identity flows):
```java
// User identified by @RequestHeader("X-User-Id") String username
// Then looked up via UserMapper
@PostMapping
public Result<Void> borrow(@RequestBody Map<String, Long> params,
                           @RequestHeader("X-User-Id") String username) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 后端 — AI 图书推荐服务 + 控制器</name>
  <files>
    backend/src/main/java/com/library/service/impl/RecommendService.java,
    backend/src/main/java/com/library/controller/RecommendController.java
  </files>
  <read_first>
    <file path="/Users/ghost/Code/Homework/web-end/backend/src/main/java/com/library/common/TfidfUtil.java">本计划依赖的 TF-IDF 工具类（由 Plan 01 创建），确认 tokenize()、computeTfidfVectors()、cosineSimilarity() 方法签名</file>
    <file path="/Users/ghost/Code/Homework/web-end/backend/src/main/java/com/library/entity/Book.java">确认 Book 实体字段名（id, title, author, description等）</file>
    <file path="/Users/ghost/Code/Homework/web-end/backend/src/main/java/com/library/mapper/BookMapper.java">确认 BookMapper 接口</file>
    <file path="/Users/ghost/Code/Homework/web-end/backend/src/main/java/com/library/mapper/BorrowMapper.java">确认 BorrowMapper 接口，特别是 selectByUserId() 方法</file>
    <file path="/Users/ghost/Code/Homework/web-end/backend/src/main/java/com/library/controller/BorrowController.java">了解 @RequestHeader("X-User-Id") 的用户身份获取模式</file>
  </read_first>
  <action>

**步骤 1：创建 `backend/src/main/java/com/library/service/impl/RecommendService.java`**

该服务提供三种推荐策略：
1. **内容推荐**：给定一本书，用 TF-IDF 余弦相似度找最相似的 N 本书
2. **个性化推荐**：根据用户借阅历史，找到相似图书
3. **热门推荐**：借阅次数最多的图书（兜底策略，当用户无借阅历史时使用）

```java
package com.library.service.impl;

import com.library.common.TfidfUtil;
import com.library.entity.Book;
import com.library.entity.Borrow;
import com.library.mapper.BookMapper;
import com.library.mapper.BorrowMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import jakarta.annotation.PostConstruct;
import java.util.*;
import java.util.stream.Collectors;

@Service
public class RecommendService {

    @Autowired
    private BookMapper bookMapper;

    @Autowired
    private BorrowMapper borrowMapper;

    /** 缓存：图书 ID（字符串） -> TF-IDF 向量 */
    private Map<String, Map<String, Double>> bookVectors = new LinkedHashMap<>();
    /** 缓存：图书 ID（字符串） -> Book 对象 */
    private Map<String, Book> bookCache = new LinkedHashMap<>();
    private volatile boolean initialized = false;

    @PostConstruct
    public void init() {
        buildIndex();
    }

    /**
     * 从数据库加载所有未删除图书，构建 TF-IDF 向量索引
     */
    public synchronized void buildIndex() {
        List<Book> books = bookMapper.selectList(null);
        Map<String, List<String>> docs = new LinkedHashMap<>();
        bookCache.clear();
        for (Book book : books) {
            String docId = book.getId().toString();
            String text = String.join(" ",
                    book.getTitle() != null ? book.getTitle() : "",
                    book.getAuthor() != null ? book.getAuthor() : "",
                    book.getDescription() != null ? book.getDescription() : ""
            );
            docs.put(docId, TfidfUtil.tokenize(text));
            bookCache.put(docId, book);
        }
        this.bookVectors = TfidfUtil.computeTfidfVectors(docs);
        this.initialized = true;
    }

    /**
     * 基于内容相似度推荐：给定图书 ID，返回最相似的 topN 本书（排除自身）
     * @param bookId 源图书 ID
     * @param topN 返回结果数量
     * @return 图书+相似度分数列表
     */
    public List<Map<String, Object>> recommendByBook(Long bookId, int topN) {
        if (!initialized) buildIndex();
        String sourceId = bookId.toString();
        Map<String, Double> sourceVec = bookVectors.get(sourceId);
        if (sourceVec == null || sourceVec.isEmpty()) {
            return getHotBooks(topN);
        }
        List<Map.Entry<String, Double>> scored = new ArrayList<>();
        for (Map.Entry<String, Map<String, Double>> entry : bookVectors.entrySet()) {
            if (entry.getKey().equals(sourceId)) continue; // 排除自身
            double sim = TfidfUtil.cosineSimilarity(sourceVec, entry.getValue());
            if (sim > 0) {
                scored.add(new AbstractMap.SimpleEntry<>(entry.getKey(), sim));
            }
        }
        scored.sort((a, b) -> Double.compare(b.getValue(), a.getValue()));
        return toResult(scored, topN);
    }

    /**
     * 基于用户借阅历史推荐：
     * 找出用户借过的所有书，对每本未借过的书计算与已借书的平均相似度
     * 按平均相似度降序排列返回 topN
     * @param userId 用户 ID
     * @param topN 返回结果数量
     */
    public List<Map<String, Object>> recommendByHistory(Long userId, int topN) {
        if (!initialized) buildIndex();
        // 获取用户借阅过的图书 ID 集合
        List<Borrow> borrows = borrowMapper.selectByUserId(userId);
        Set<String> borrowedBookIds = borrows.stream()
                .map(b -> b.getBookId().toString())
                .collect(Collectors.toSet());
        if (borrowedBookIds.isEmpty()) {
            return getHotBooks(topN);
        }
        // 获取已借书的 TF-IDF 向量集合
        List<Map<String, Double>> borrowedVecs = new ArrayList<>();
        for (String bid : borrowedBookIds) {
            Map<String, Double> vec = bookVectors.get(bid);
            if (vec != null && !vec.isEmpty()) {
                borrowedVecs.add(vec);
            }
        }
        if (borrowedVecs.isEmpty()) {
            return getHotBooks(topN);
        }
        // 对每本未借过的书，计算与所有已借书的平均相似度
        List<Map.Entry<String, Double>> scored = new ArrayList<>();
        for (Map.Entry<String, Map<String, Double>> entry : bookVectors.entrySet()) {
            if (borrowedBookIds.contains(entry.getKey())) continue;
            double totalSim = 0.0;
            for (Map<String, Double> bv : borrowedVecs) {
                totalSim += TfidfUtil.cosineSimilarity(entry.getValue(), bv);
            }
            double avgSim = totalSim / borrowedVecs.size();
            if (avgSim > 0) {
                scored.add(new AbstractMap.SimpleEntry<>(entry.getKey(), avgSim));
            }
        }
        scored.sort((a, b) -> Double.compare(b.getValue(), a.getValue()));
        // 如果相似度推荐结果不够，用热门图书补齐
        List<Map<String, Object>> results = toResult(scored, topN);
        if (results.size() < topN) {
            Set<String> existingIds = results.stream()
                    .map(r -> r.get("id").toString()).collect(Collectors.toSet());
            List<Map<String, Object>> hotBooks = getHotBooks(topN);
            for (Map<String, Object> hot : hotBooks) {
                if (results.size() >= topN) break;
                if (!existingIds.contains(hot.get("id").toString()) && !borrowedBookIds.contains(hot.get("id").toString())) {
                    results.add(hot);
                }
            }
        }
        return results;
    }

    /**
     * 热门图书：按借阅次数降序排列
     * 统计 sys_borrow 表中每本图书的被借次数
     */
    public List<Map<String, Object>> getHotBooks(int topN) {
        List<Borrow> allBorrows = borrowMapper.selectAll();
        Map<Long, Long> borrowCounts = allBorrows.stream()
                .collect(Collectors.groupingBy(Borrow::getBookId, Collectors.counting()));
        // 按借阅次数降序排序
        List<Map.Entry<Long, Long>> sorted = borrowCounts.entrySet().stream()
                .sorted(Map.Entry.<Long, Long>comparingByValue().reversed())
                .collect(Collectors.toList());
        List<Map<String, Object>> results = new ArrayList<>();
        for (Map.Entry<Long, Long> entry : sorted) {
            if (results.size() >= topN) break;
            Book book = bookCache.get(entry.getKey().toString());
            if (book == null) {
                book = bookMapper.selectById(entry.getKey());
                if (book != null) bookCache.put(entry.getKey().toString(), book);
            }
            if (book != null) {
                Map<String, Object> item = bookToMap(book);
                item.put("borrowCount", entry.getValue());
                item.put("score", (double) entry.getValue()); // 借阅次数作为分数
                results.add(item);
            }
        }
        return results;
    }

    /**
     * 将评分结果转换为 API 返回格式
     */
    private List<Map<String, Object>> toResult(List<Map.Entry<String, Double>> scored, int topN) {
        List<Map<String, Object>> results = new ArrayList<>();
        for (Map.Entry<String, Double> entry : scored) {
            if (results.size() >= topN) break;
            Book book = bookCache.get(entry.getKey());
            if (book != null) {
                Map<String, Object> item = bookToMap(book);
                item.put("score", Math.round(entry.getValue() * 10000.0) / 10000.0);
                results.add(item);
            }
        }
        return results;
    }

    private Map<String, Object> bookToMap(Book book) {
        Map<String, Object> item = new LinkedHashMap<>();
        item.put("id", book.getId());
        item.put("title", book.getTitle());
        item.put("author", book.getAuthor());
        item.put("isbn", book.getIsbn());
        item.put("categoryId", book.getCategoryId());
        item.put("description", book.getDescription());
        item.put("coverUrl", book.getCoverUrl());
        item.put("stock", book.getStock());
        item.put("status", book.getStatus());
        item.put("categoryName", book.getCategoryName());
        return item;
    }
}
```

**步骤 2：创建 `backend/src/main/java/com/library/controller/RecommendController.java`**

```java
package com.library.controller;

import com.library.common.Result;
import com.library.entity.User;
import com.library.mapper.UserMapper;
import com.library.service.impl.RecommendService;
import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

import java.util.List;
import java.util.Map;

@RestController
@RequestMapping("/api/ai")
public class RecommendController {

    @Autowired
    private RecommendService recommendService;

    @Autowired
    private UserMapper userMapper;

    /**
     * 基于内容的相似图书推荐
     * GET /api/ai/recommend/book/{id}?topN=6
     */
    @GetMapping("/recommend/book/{id}")
    public Result<List<Map<String, Object>>> recommendByBook(
            @PathVariable Long id,
            @RequestParam(defaultValue = "6") int topN) {
        if (topN > 20) topN = 20; // 上限保护
        return Result.success(recommendService.recommendByBook(id, topN));
    }

    /**
     * 基于用户借阅历史的个性化推荐
     * GET /api/ai/recommend/user?topN=6
     */
    @GetMapping("/recommend/user")
    public Result<List<Map<String, Object>>> recommendByHistory(
            @RequestHeader("X-User-Id") String username,
            @RequestParam(defaultValue = "6") int topN) {
        if (topN > 20) topN = 20;
        User user = userMapper.selectOne(
                new LambdaQueryWrapper<User>().eq(User::getUsername, username)
        );
        if (user == null) {
            return Result.error(404, "用户不存在");
        }
        return Result.success(recommendService.recommendByHistory(user.getId(), topN));
    }

    /**
     * 热门图书排行榜
     * GET /api/ai/hot?topN=10
     */
    @GetMapping("/hot")
    public Result<List<Map<String, Object>>> hotBooks(
            @RequestParam(defaultValue = "10") int topN) {
        if (topN > 50) topN = 50;
        return Result.success(recommendService.getHotBooks(topN));
    }
}
```
</action>
  <acceptance_criteria>
    <criterion type="grep">backend/src/main/java/com/library/service/impl/RecommendService.java 包含 "recommendByBook"</criterion>
    <criterion type="grep">backend/src/main/java/com/library/service/impl/RecommendService.java 包含 "recommendByHistory"</criterion>
    <criterion type="grep">backend/src/main/java/com/library/service/impl/RecommendService.java 包含 "getHotBooks"</criterion>
    <criterion type="grep">backend/src/main/java/com/library/service/impl/RecommendService.java 包含 "TfidfUtil.cosineSimilarity"</criterion>
    <criterion type="grep">backend/src/main/java/com/library/service/impl/RecommendService.java 包含 "buildIndex"</criterion>
    <criterion type="grep">backend/src/main/java/com/library/controller/RecommendController.java 包含 "recommend/book/{id}"</criterion>
    <criterion type="grep">backend/src/main/java/com/library/controller/RecommendController.java 包含 "recommend/user"</criterion>
    <criterion type="grep">backend/src/main/java/com/library/controller/RecommendController.java 包含 "/hot"</criterion>
    <criterion type="grep">backend/src/main/java/com/library/controller/RecommendController.java 包含 "X-User-Id"</criterion>
    <criterion type="grep">backend/src/main/java/com/library/controller/RecommendController.java 包含 "RecommendService"</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ghost/Code/Homework/web-end/backend && mvn compile -q 2>&1</automated>
  </verify>
  <done>RecommendService 和 RecommendController 创建完毕，mvn compile 通过（BUILD SUCCESS），3 个 API 端点可访问</done>
</task>

<task type="auto">
  <name>Task 2: 前端 — 图书推荐页面 + 图书详情页推荐区块</name>
  <files>
    frontend/src/api/recommend.js,
    frontend/src/views/ai/Recommend.vue,
    frontend/src/views/book/Detail.vue
  </files>
  <read_first>
    <file path="/Users/ghost/Code/Homework/web-end/frontend/src/api/book.js">了解 API 模块的函数签名模式</file>
    <file path="/Users/ghost/Code/Homework/web-end/frontend/src/views/book/Detail.vue">了解图书详情页的现有结构，找到添加推荐区块的最佳位置</file>
    <file path="/Users/ghost/Code/Homework/web-end/frontend/src/views/book/Index.vue">了解 Element Plus 卡片/表格组件在图书场景中的使用模式（el-table, el-tag, el-link等）</file>
    <file path="/Users/ghost/Code/Homework/web-end/frontend/src/stores/user.js">了解 userStore 中 userInfo 的结构（包含 id, username, roleId等）</file>
  </read_first>
  <action>

**步骤 1：创建 `frontend/src/api/recommend.js`**

```javascript
import request from './request'

/**
 * 基于内容的相似图书推荐
 * @param {number} bookId - 图书 ID
 * @param {number} topN - 返回数量，默认 6
 * @returns {Promise} - data: [{ id, title, author, isbn, categoryId, description, coverUrl, stock, status, score }]
 */
export function getSimilarBooks(bookId, topN = 6) {
  return request({ url: `/ai/recommend/book/${bookId}`, method: 'get', params: { topN } })
}

/**
 * 基于用户借阅历史的个性化推荐
 * @param {number} topN - 返回数量，默认 6
 * @returns {Promise}
 */
export function getRecommendForUser(topN = 6) {
  return request({ url: '/ai/recommend/user', method: 'get', params: { topN } })
}

/**
 * 热门图书排行榜
 * @param {number} topN - 返回数量，默认 10
 * @returns {Promise} - data: [{ id, title, author, ..., borrowCount, score }]
 */
export function getHotBooks(topN = 10) {
  return request({ url: '/ai/hot', method: 'get', params: { topN } })
}
```

**步骤 2：创建 `frontend/src/views/ai/Recommend.vue`**

该页面包含两个区域：
- 上方：个性化推荐（基于借阅历史，需要登录用户）
- 下方：热门图书排行榜

```vue
<template>
  <div class="page-container">
    <div class="page-card">
      <div class="page-card-header">
        <span class="page-title">AI 图书推荐</span>
        <span style="font-size:13px;color:var(--color-text-muted);margin-left:12px">
          基于 TF-IDF 内容相似度和借阅历史智能推荐
        </span>
      </div>
      <div class="page-card-body">
        <!-- 个性化推荐区域 -->
        <h3 style="margin-bottom:16px;font-size:16px;font-weight:600">
          <el-icon style="margin-right:6px;vertical-align:middle"><Star /></el-icon>
          猜你喜欢
        </h3>
        <el-row v-if="personalRecs.length > 0" :gutter="16">
          <el-col v-for="book in personalRecs" :key="book.id" :span="6" style="margin-bottom:16px">
            <el-card shadow="hover" @click="$router.push(`/book/${book.id}`)" style="cursor:pointer">
              <div style="font-size:15px;font-weight:600;margin-bottom:6px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap">
                {{ book.title }}
              </div>
              <div style="font-size:13px;color:var(--color-text-muted);margin-bottom:4px">{{ book.author || '未知作者' }}</div>
              <el-tag size="small" type="success">相似度 {{ (book.score * 100).toFixed(0) }}%</el-tag>
              <el-tag size="small" style="margin-left:6px" v-if="book.stock > 0" type="warning">库存 {{ book.stock }}</el-tag>
              <el-tag size="small" style="margin-left:6px" v-else type="danger">已借完</el-tag>
            </el-card>
          </el-col>
        </el-row>
        <el-empty v-else description="借阅更多图书后，将为您生成个性化推荐" :image-size="80" />

        <!-- 分隔线 -->
        <el-divider style="margin:24px 0" />

        <!-- 热门图书排行榜 -->
        <h3 style="margin-bottom:16px;font-size:16px;font-weight:600">
          <el-icon style="margin-right:6px;vertical-align:middle"><TrendCharts /></el-icon>
          热门图书排行榜
        </h3>
        <el-table v-if="hotBooks.length > 0" :data="hotBooks" stripe>
          <el-table-column type="index" label="排名" width="60" />
          <el-table-column prop="title" label="书名" min-width="160">
            <template #default="{ row }">
              <el-link type="primary" @click="$router.push(`/book/${row.id}`)">{{ row.title }}</el-link>
            </template>
          </el-table-column>
          <el-table-column prop="author" label="作者" width="130" />
          <el-table-column prop="stock" label="库存" width="60" />
          <el-table-column label="被借次数" width="100">
            <template #default="{ row }">
              <el-tag type="warning" size="small">{{ row.borrowCount || 0 }} 次</el-tag>
            </template>
          </el-table-column>
        </el-table>
        <el-empty v-else description="暂无借阅数据" :image-size="80" />
      </div>
    </div>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import { Star, TrendCharts } from '@element-plus/icons-vue'
import { ElMessage } from 'element-plus'
import { getRecommendForUser, getHotBooks } from '@/api/recommend'

const personalRecs = ref([])
const hotBooks = ref([])

onMounted(async () => {
  try {
    const res = await getRecommendForUser(8)
    if (res.code === 200) {
      personalRecs.value = res.data || []
    }
  } catch (e) {
    console.error('Failed to load recommendations:', e)
  }

  try {
    const res = await getHotBooks(10)
    if (res.code === 200) {
      hotBooks.value = res.data || []
    }
  } catch (e) {
    console.error('Failed to load hot books:', e)
  }
})
</script>
```

**步骤 3：修改 `frontend/src/views/book/Detail.vue` — 在页面底部添加"相似图书"推荐区块**

在现有 `</template>` 关闭标签之前、最后一个内容块之后，添加：

```html
<!-- AI 相似图书推荐 -->
<div v-if="similarBooks.length > 0" style="margin-top:24px">
  <el-divider />
  <h3 style="margin-bottom:16px;font-size:16px;font-weight:600">
    <el-icon style="margin-right:6px;vertical-align:middle"><Star /></el-icon>
    猜你喜欢（相似图书）
  </h3>
  <el-row :gutter="16">
    <el-col v-for="book in similarBooks" :key="book.id" :span="8" style="margin-bottom:16px">
      <el-card shadow="hover" @click="$router.push(`/book/${book.id}`)" style="cursor:pointer">
        <div style="font-size:14px;font-weight:600;margin-bottom:6px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap">
          {{ book.title }}
        </div>
        <div style="font-size:12px;color:var(--color-text-muted);margin-bottom:4px">{{ book.author || '未知作者' }}</div>
        <el-tag size="small" type="success">相似 {{ (book.score * 100).toFixed(0) }}%</el-tag>
      </el-card>
    </el-col>
  </el-row>
</div>
```

在 `<script setup>` 中添加（与其他 import 和逻辑一起）：

```javascript
// 在现有 import 之后添加：
import { getSimilarBooks } from '@/api/recommend'
import { Star } from '@element-plus/icons-vue'

// 在现有 ref 声明之后添加：
const similarBooks = ref([])

// 在 onMounted 或现有的数据加载逻辑中（获取图书详情成功后）添加：
const loadSimilarBooks = async () => {
  try {
    const res = await getSimilarBooks(route.params.id, 6)
    if (res.code === 200) {
      similarBooks.value = res.data || []
    }
  } catch (e) {
    console.error('Failed to load similar books:', e)
  }
}
// 在获取图书详情的 then/catch 块之后调用 loadSimilarBooks()
```

注意：图书详情页的 `route.params.id` 已经可用（因为路由中已定义 `/book/:id`）。在现有获取详情的成功回调中，紧接着调用 `loadSimilarBooks()`。
</action>
  <acceptance_criteria>
    <criterion type="grep">frontend/src/api/recommend.js 包含 "export function getSimilarBooks"</criterion>
    <criterion type="grep">frontend/src/api/recommend.js 包含 "export function getRecommendForUser"</criterion>
    <criterion type="grep">frontend/src/api/recommend.js 包含 "export function getHotBooks"</criterion>
    <criterion type="grep">frontend/src/views/ai/Recommend.vue 包含 "getRecommendForUser"</criterion>
    <criterion type="grep">frontend/src/views/ai/Recommend.vue 包含 "getHotBooks"</criterion>
    <criterion type="grep">frontend/src/views/ai/Recommend.vue 包含 "猜你喜欢"</criterion>
    <criterion type="grep">frontend/src/views/ai/Recommend.vue 包含 "热门图书排行榜"</criterion>
    <criterion type="grep">frontend/src/views/book/Detail.vue 包含 "getSimilarBooks"</criterion>
    <criterion type="grep">frontend/src/views/book/Detail.vue 包含 "猜你喜欢（相似图书）"</criterion>
    <criterion type="grep">frontend/src/views/book/Detail.vue 包含 "@/api/recommend"</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ghost/Code/Homework/web-end/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>推荐页面包含个性化推荐 + 热门排行榜两个区域；图书详情页底部显示相似图书推荐区块；npm run build 成功</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> GET /api/ai/recommend/book/{id} | 客户端传入任意图书 ID，后端查询相似图书（只读操作） |
| Client -> GET /api/ai/recommend/user | 需要认证，通过 X-User-Id header 标识用户身份 |
| Client -> GET /api/ai/hot | 公开接口，任何人可查看热门图书排行榜 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-04 | Information Disclosure | RecommendController.recommendByHistory() | mitigate | 通过 @RequestHeader("X-User-Id") 获取用户名并查找用户，未找到时返回 404；防止用户枚举攻击 |
| T-02.1-05 | Denial of Service | RecommendController.recommendByBook() | mitigate | topN 参数上限设置为 20（代码中 if (topN > 20) topN = 20），防止单次请求返回过多数据 |
| T-02.1-06 | Denial of Service | RecommendController.hotBooks() | mitigate | topN 参数上限设置为 50，限制返回数量 |
| T-02.1-07 | Information Disclosure | RecommendController.hotBooks() | accept | 热门图书排行榜为公开信息，不包含用户个人数据，低风险 |
</threat_model>

<verification>
整体验证步骤：
1. 后端编译：`cd backend && mvn compile -q` 应输出 BUILD SUCCESS
2. 前端构建：`cd frontend && npm run build` 应成功完成
3. API 测试：
   - `curl -X GET http://localhost:8080/api/ai/recommend/book/1?topN=3` 返回相似图书列表
   - `curl -X GET http://localhost:8080/api/ai/hot?topN=5` 返回热门图书排行榜
   - `curl -X GET http://localhost:8080/api/ai/recommend/user?topN=3 -H "X-User-Id: admin"` 返回个性化推荐
</verification>

<success_criteria>
1. `mvn compile -q` 返回 BUILD SUCCESS
2. `npm run build` 成功完成
3. GET /api/ai/recommend/book/1?topN=3 返回 code=200，data 数组中每项包含 id, title, author, score 字段，且不包含 id=1 的图书自身
4. GET /api/ai/hot?topN=5 返回借阅次数最多的图书，data 中包含 borrowCount 字段
5. 图书推荐页面（/ai/recommend）正常渲染个性化推荐和热门排行榜
6. 图书详情页底部显示相似图书推荐区块
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-ai/02.1-02-SUMMARY.md`
</output>
